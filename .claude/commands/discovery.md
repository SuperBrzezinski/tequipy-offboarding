---
description: Run the discovery session to resolve ambiguous flows before the spec.
---

Run the `discovery-session` skill. Spawn `it-admin-domain-expert`, work the seeded question
bank in `active-context.md` one question at a time (propose a default marked as an
assumption, give the trade-off, ask the operator to confirm), decide the one bonus feature,
and record decisions in `docs/discovery.md` (ADR-flag anything that interprets the task).
