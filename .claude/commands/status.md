---
description: Print the current project state from memory (no changes).
---

Read and summarize, without modifying anything:
- `agent/memory/active-context.md` — phase, active epic, last checkpoint, next action, open questions
- the top of `agent/memory/progress.md` — last few increments
- the active epic's tasks in `agent/memory/backlog.md`

Then state the single recommended next action and which skill/agent it implies.
