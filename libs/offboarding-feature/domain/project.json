{
  "name": "offboarding-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/offboarding-feature/domain/src",
  "projectType": "library",
  "tags": ["type:domain", "scope:offboarding"],
  "targets": {
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "npx tsc --noEmit --project tsconfig.spec.json"
      }
    }
  }
}
